Class {
	#name : #RTConstraintTest,
	#superclass : #TRConstraintTest,
	#category : #'Roassal2-Core-Tests'
}

{ #category : #running }
RTConstraintTest >> setUp [
	super setUp.
	shape1 := (RTBox new size: 50) element.
	shape2 := (RTBox new size: 10) element.
	
	
]
